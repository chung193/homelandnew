'use client';
import Image from 'next/image';import {usePathname,useSearchParams} from 'next/navigation';import {useRef} from 'react';import type {Locale} from '../../i18n/config';const options:Array<{locale:Locale;label:string;flag:string}>=[{locale:'vi',label:'Tiếng Việt',flag:'/vi.png'},{locale:'en',label:'English',flag:'/en.png'}];
export default function LocaleSwitchButton({currentLocale}:{currentLocale:Locale}){const pathname=usePathname();const searchParams=useSearchParams();const detailsRef=useRef<HTMLDetailsElement>(null);const current=options.find(option=>option.locale===currentLocale)??options[0];function switchLocale(locale:Locale){if(locale===currentLocale){detailsRef.current?.removeAttribute('open');return}sessionStorage.setItem('homelend:scrollY',String(window.scrollY));const localizedPath=pathname.replace(/^\/(vi|en)(?=\/|$)/,`/${locale}`);const targetPath=localizedPath===pathname&&!/^\/(vi|en)(?=\/|$)/.test(pathname)?`/${locale}${pathname}`:localizedPath;const query=searchParams.toString();window.location.assign(query?`${targetPath}?${query}`:targetPath)}return <details ref={detailsRef} className="relative shrink-0"><summary className="topbar-select flex h-9 cursor-pointer list-none items-center gap-2 rounded-lg border px-2 text-sm"><Image src={current.flag} alt="" width={24} height={16}/><span className="hidden lg:inline">{current.label}</span></summary><div className="account-menu-panel absolute right-0 z-50 mt-2 min-w-40 rounded-xl border p-1.5">{options.map(option=><button key={option.locale} type="button" onClick={()=>switchLocale(option.locale)} className="flex w-full items-center gap-3 rounded-lg px-3 py-2 text-left text-sm"><Image src={option.flag} alt="" width={28} height={19}/><span>{option.label}</span></button>)}</div></details>}
