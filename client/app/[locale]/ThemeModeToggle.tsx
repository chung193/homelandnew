'use client';
import {useRef} from 'react';import {Icon,type IconName} from '@astryxdesign/core/Icon';import {useThemeMode,type UIMode} from '../providers';type Labels={system:string;light:string;dark:string;night:string};const MODES:UIMode[]=['system','light','dark','night'];const ICONS:Record<UIMode,IconName>={system:'viewColumns',light:'success',dark:'eyeSlash',night:'clock'};
export default function ThemeModeToggle({labels}:{labels:Labels}){const{mode,setMode}=useThemeMode();const ref=useRef<HTMLDetailsElement>(null);return <details ref={ref} className="relative"><summary className="topbar-select flex h-9 cursor-pointer list-none items-center gap-2 rounded-lg border px-2 text-sm"><Icon icon={ICONS[mode]} size="sm"/><span className="hidden xl:inline">{labels[mode]}</span></summary><div className="account-menu-panel absolute right-0 z-50 mt-2 min-w-44 rounded-xl border p-1">{MODES.map(candidate=><button key={candidate} type="button" onClick={()=>{setMode(candidate);ref.current?.removeAttribute('open')}} className="topbar-link flex w-full items-center gap-3 rounded-lg px-3 py-2 text-left text-sm"><Icon icon={ICONS[candidate]} size="sm"/><span>{labels[candidate]}</span></button>)}</div></details>}
