export type BillingUnit='month'|'day'|'night';
export type Pricing={units:number;usesLongTerm:boolean;appliedPrice:number;rentTotal:number;deposit:number;payableTotal:number};
export function normalizePriceUnit(value:string):BillingUnit{const unit=value.trim().toLowerCase();if(['month','months','monthly','tháng','thang'].includes(unit))return'month';if(['day','days','daily','ngày','ngay'].includes(unit))return'day';return'night'}
export function addCalendarMonths(value:string,months:number):string{if(!value)return'';const[y,m,d]=value.split('-').map(Number);const target=new Date(Date.UTC(y,m-1+months,1));const last=new Date(Date.UTC(target.getUTCFullYear(),target.getUTCMonth()+1,0)).getUTCDate();target.setUTCDate(Math.min(d,last));return target.toISOString().slice(0,10)}
export function daysBetween(start:string,end:string):number|null{if(!start||!end)return null;const days=(Date.parse(`${end}T00:00:00Z`)-Date.parse(`${start}T00:00:00Z`))/86400000;return days>0?days:null}
export function calculatePricing(input:{unitPrice:number|null;nights:number|null;billingUnit:BillingUnit;rentalMonths:number;longTermMonths:number|null;longTermPrice:string|null;depositAmount:string|null}):Pricing|null{if(input.unitPrice===null||input.nights===null)return null;const units=input.billingUnit==='month'?input.rentalMonths:input.nights;const promo=input.longTermPrice===null?null:Number(input.longTermPrice);const usesLongTerm=input.billingUnit==='month'&&input.longTermMonths!==null&&promo!==null&&Number.isFinite(promo)&&units>=input.longTermMonths;const appliedPrice=usesLongTerm?promo:input.unitPrice;const rentTotal=appliedPrice*units;const deposit=Number(input.depositAmount)||0;return{units,usesLongTerm,appliedPrice,rentTotal,deposit,payableTotal:rentTotal+deposit}}
